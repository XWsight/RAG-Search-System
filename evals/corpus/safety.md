# 隐私与安全边界

上传的文档和检索片段都应视为不可信输入。文档中可能包含提示注入文本、脚本、异常压缩包或超大内容。加载器需要限制文件类型、文件大小、解压后大小、页数和字符数，并拒绝符号链接、路径穿越和危险 URL。

本地检索不需要把内容发送到网络。只有在用户明确同意时，系统才应把问题交给联网搜索，或把检索证据交给云端生成服务。界面应分别提供这两个开关，默认关闭，并说明数据会发送给第三方服务。

日志和错误信息不得包含 API Key、完整上游响应或用户私有文档。外部调用应设置连接超时、读取超时和有限重试，且只重试临时错误。
