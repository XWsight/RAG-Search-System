# 混合检索与排序

稀疏检索以词项匹配为核心。BM25 对产品编号、函数名、缩写和罕见专有名词等精确关键词很敏感。稠密检索把问题和文本编码为向量，适合召回表达不同但语义接近的内容。

混合检索同时运行 BM25 与稠密向量检索。两种分数通常不在同一尺度上，因此可以使用 Reciprocal Rank Fusion，也就是 RRF，按照名次而不是原始分数融合候选列表。随后可以使用交叉编码器做精排，并进行来源多样化，减少多个近重复片段占满上下文。

检索阈值不应该凭感觉固定。应在带标注的数据集上测量 Recall@k、MRR 和 nDCG，再根据漏答与误答成本校准路由阈值。
