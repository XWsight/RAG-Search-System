# 向量索引与生命周期

Embedding 模型把文本映射成固定维度的数值向量。语义接近的文本通常在向量空间里更靠近。ChromaDB 可以保存向量、文本和元数据，并根据查询向量执行相似性搜索。

索引应在文档变化后构建一次，并在多次提问之间复用。每个问题都重新切分、向量化和创建集合会浪费时间与计算资源，还可能在长时间运行时累积未释放的集合。稳定内容哈希可以用于复用相同文档集合的索引。

临时索引也需要生命周期管理，例如会话过期、LRU 容量限制和显式关闭。上传资料是否持久化应由清晰的隐私策略决定。
