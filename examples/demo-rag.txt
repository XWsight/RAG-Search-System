RAG（检索增强生成）是一种让大模型结合外部资料回答问题的方法。

它通常包含四个步骤：首先把文档切分成较小的文本片段；然后使用嵌入模型把每个片段转换为向量；接着根据用户问题检索最相关的片段；最后把这些片段和问题一起交给语言模型生成回答。

向量数据库用于保存文本片段及其向量。它可以根据语义相似度找到与问题最相关的资料，而不只是匹配完全相同的关键词。

混合检索会同时结合关键词检索和向量检索：关键词检索擅长专有名词、编号和精确词汇；向量检索擅长意思相近但表达不同的句子。

高质量的 RAG 系统应当展示引用来源，并在资料不足时明确说明不知道，而不是编造答案。
